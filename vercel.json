{
    "build": {
      "commands": [
        "npm install --save-dev @angular/cli",
        "npm run build"
      ]
    },
    "rewrites": [
      {
        "source": "/:match*",
        "destination": "/index.html"
      }
    ]
  }
  